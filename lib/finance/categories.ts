/**
 * Starting categories, not a forced template — the category field is a
 * plain string, so these are just suggestions (via a <datalist>); the user
 * can type anything and the set naturally grows to fit actual spending.
 */
export const DEFAULT_CATEGORIES = ["Housing", "Food", "Transport", "Shopping", "Entertainment", "Other"];
